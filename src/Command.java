public interface Command {
    /***
     * Method to execute a specified command in the hurricane prediction sequence for class implementing it.
     * (i.e. Applying the prediction model)
     */
    void execute();
}
